# Iter-9 Run Record — 2026-05-19

- Surface: `Field` list choice rendering in settings forms at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-9-baseline/` reported 178 passed, 9 failed.
- RED commit: b89f206 `test(ui-auto/iter-9): expose settings field CJK choice overflow`
- GREEN commit: 0ed3321 `fix(tui/settings): truncate list choices by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-9-red-check/` reported the new `SettingsFieldCjkChoice / cjk-list-choice-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 178 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-9-source-red-check/` reported the same 178 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Submenu/settings/Field.test.tsx` passed 4/4.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-9-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 180 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-9-source-green-check/` reported the same 180 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-9-settings-field-cjk-choice.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-9-settings-field-capture-green/` showed the CJK choice ending with an ellipsis inside the settings field border.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
