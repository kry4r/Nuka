# Iter-11 Run Record — 2026-05-19

- Surface: `SubmenuList` right-aligned value summary at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-11-baseline/` reported 182 passed, 9 failed.
- RED commit: d677bfa `test(ui-auto/iter-11): expose SubmenuList CJK value overflow`
- GREEN commit: 3712bd5 `fix(tui/submenu): truncate value summaries by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-11-red-check/` reported the new `SubmenuListCjkValue / cjk-value-summary-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 182 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-11-source-red-check/` reported the same 182 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/submenuList.test.tsx` passed 16/16.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-11-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 184 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-11-source-green-check/` reported the same 184 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-11-submenu-list-cjk-value.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-11-submenu-list-capture-green/` showed the CJK value summary ending with an ellipsis inside the fixed value column.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires `stringWidth` / `truncateByWidth` for this class of visible terminal truncation.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
