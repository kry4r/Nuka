# Iter-6 Run Record — 2026-05-19

- Surface: `ToolCall` progress box width at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-6-baseline/` reported 170 passed, 9 failed.
- RED commit: 8c92ab4 `test(ui-auto/iter-6): expose ToolCall CJK progress overflow`
- GREEN commit: 841308b `fix(tui/messages): truncate tool progress lines by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-6-red-check/` reported the new `ToolCallCjkProgress / cjk-progress-line-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 172 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/toolCall.test.tsx` passed 12/12.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-6-green-check/` reported 174 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-6-source-green-check/` reported the same 174 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-6-tool-call-cjk-progress.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-6-toolcall-capture-green/` showed the progress line ending with an ellipsis inside the bordered box.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
