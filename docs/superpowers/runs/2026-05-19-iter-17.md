# Iter-17 Run Record — 2026-05-19

- Surface: Welcome `Feed` timestamp gutter with CJK timestamps at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-17-baseline/` reported 194 passed, 9 failed.
- RED commit: 083cbe0 `test(ui-auto/iter-17): expose Feed CJK timestamp gutter wrap`
- GREEN commit: f80eae9 `fix(tui/welcome): pad Feed timestamps by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-17-red/` reported `FeedCjkTimestamp / cjk-timestamp-gutter-pads-by-display-width` failing at 60×20 and 70×20; total moved to 194 passed, 11 failed.
- Capture evidence before the fix: `nuka explore capture test/ui-auto/fixtures/iter-17-feed-cjk-timestamp.fixtures.tsx --viewport=60x20 --out=.ink-explorer/iter-17-feed-capture/` showed the CJK timestamp on one row and the feed text on the next row.
- Fix: replaced `String.padEnd()` in the timestamp gutter with shared `padToWidth()` so gutter padding is measured in terminal display cells.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Feed.test.tsx` passed 18/18.
- Trellis check: `npm test -- test/tui/Feed.test.tsx test/core/stringWidth/stringWidth.test.ts` passed 69/69; `npm run build` passed; `git diff --check` passed.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-17-green/` reported the new Feed case passing at 60×20 and 70×20; total returned to 196 passed, 9 failed.
- Capture check after the fix: `nuka explore capture test/ui-auto/fixtures/iter-17-feed-cjk-timestamp.fixtures.tsx --viewport=60x20 --out=.ink-explorer/iter-17-green-capture/` showed timestamp and truncated text on the same row.
- Spec update: none needed; `.trellis/spec/frontend/quality-guidelines.md` already requires display-width helpers for visible terminal alignment.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
