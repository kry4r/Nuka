# Iter-12 Run Record — 2026-05-19

- Surface: `CommandList` slash command name column at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-12-baseline/` reported 184 passed, 9 failed.
- RED commit: adfc1d4 `test(ui-auto/iter-12): expose SlashCard CJK command overflow`
- GREEN commit: 6921059 `fix(tui/slash): truncate command rows by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-12-red-check-2/` reported the new `SlashCommandCjkName / cjk-name-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 184 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-12-source-red-check/` reported the same 184 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/slashCard.test.tsx test/tui/SlashCard.harness.test.tsx` passed 10/10.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-12-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 186 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-12-source-green-check/` reported the same 186 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-12-slash-command-cjk-name.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-12-slash-command-cjk-name-capture-green/` showed the CJK slash command name ending with an ellipsis inside the fixed 14-cell command-name column.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires `stringWidth` / `truncateByWidth` for this class of visible terminal truncation.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
