# Iter-15 Run Record — 2026-05-19

- Surface: `SubagentList` in-flight dispatch-agent task labels at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-15-baseline/` reported 190 passed, 9 failed.
- RED commit: 994e2f2 `test(ui-auto/iter-15): expose SubagentList CJK task overflow`
- GREEN commit: 8b31153 `fix(tui/tasks): truncate subagent list labels by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-15-red-check/` reported the new `SubagentListCjkTask / cjk-task-label-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 190 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-15-source-red-check/` reported the same 190 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Tasks/SubagentList.test.tsx test/tui/Tasks/TasksPanel.test.tsx test/tui/Tasks/Tasks.harness.test.tsx` passed 19/19.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-15-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 192 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-15-source-green-check/` reported the same 192 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-15-subagent-list-cjk-task.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-15-subagent-list-capture-green/` showed the CJK task label ending with an ellipsis on one subagent row.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires display-width helpers for visible truncation and alignment.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
