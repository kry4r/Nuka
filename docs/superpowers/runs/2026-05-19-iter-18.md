# Iter-18 Run Record — 2026-05-19

- Surface: Welcome CJK model row at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-18-baseline/` reported 196 passed, 9 failed.
- RED commit: 04151bd `test(ui-auto/iter-18): expose Welcome CJK viewport overflow`
- GREEN commit: 2a7107c `fix(tui/hooks): read Ink stdout terminal size`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-18-red/` reported `WelcomeCjkModelViewport / cjk-model-row-uses-explorer-viewport-width` failing at 60×30 and 70×30; total moved to 196 passed, 11 failed.
- Root cause: `useTerminalSize()` read `process.stdout.columns`, so explorer mounts using an injected Ink stdout still laid out Welcome from the host terminal width. The two-column layout overflowed 60/70 column captures by one terminal cell on the CJK model row.
- Fix: `useTerminalSize()` now reads Ink's `useStdout().stdout` dimensions first, subscribes to that stream's resize event, and falls back to `process.stdout` only when needed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/welcome.test.tsx test/tui/Welcome.harness.test.tsx test/tui/History/SessionList.test.tsx test/tui/Stats/StatsView.test.tsx` passed 22/22.
- Trellis check: applicable frontend spec was re-read; `git diff --check` passed.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-18-green/` reported the new Welcome fixture passing at 60×30 and 70×30; total improved to 200 passed, 7 failed.
- Capture check after the fix: `nuka explore capture test/ui-auto/fixtures/iter-18-welcome-cjk-model-viewport.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-18-green-capture/` showed compact Welcome layout inside the 60-column viewport.
- Spec update: none needed; `.trellis/spec/frontend/quality-guidelines.md` already names `useTerminalSize` and requires viewport-aware terminal width handling.
- Remaining failures are the Bug B snapshot cases only.
