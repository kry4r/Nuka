# Iter-14 Run Record — 2026-05-19

- Surface: `TokensView` monitor token rollup agent-name column at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-14-baseline/` reported 188 passed, 9 failed.
- RED commit: eb9abcb `test(ui-auto/iter-14): expose Monitor tokens CJK overflow`
- GREEN commit: e0ecf9d `fix(tui/monitor): truncate token agent names by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-14-red-check/` reported the new `MonitorTokensCjkAgent / cjk-agent-name-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 188 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-14-source-red-check/` reported the same 188 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Monitor` passed 12/12.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-14-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 190 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-14-source-green-check/` reported the same 190 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-14-monitor-tokens-cjk-agent.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-14-monitor-tokens-capture-green/` showed the CJK agent-name column ending with an ellipsis on one token row.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires display-width helpers for visible truncation and alignment.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
