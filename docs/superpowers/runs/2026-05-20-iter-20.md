# Iter-20 Run Record - 2026-05-20

- Surface: Main chat transcript and prompt input cursor, plus the `ink-ui-explorer` blind spots that missed both regressions.
- Baseline sweep: existing default sweep was green before this iteration, but it was too weak: it did not prove that completed chat turns stayed in the live viewport, and prompt fixtures did not require a positioned native terminal cursor.
- UI RED commit: 809f373 `test(tui/iter-20): expose transcript and cursor regressions`
- UI GREEN commit: dbd4d24 `fix(tui): keep transcript live and declare prompt cursor`
- Explorer RED commit: 56c3b94 `test(explorer/iter-20): require native cursor telemetry`
- Explorer GREEN commit: 8553eb3 `fix(explorer): enforce native cursor telemetry`
- Skill docs commit: f0a36db `docs(skill): document ink runtime blind spots`
- RED evidence: in `.tmp-red-ui-iter20`, `npm test -- test/tui/Messages.static.test.tsx test/tui/PromptInput.cursor.test.tsx` failed with 6 failing assertions: `<Static>` writes were captured for previous turns/prologue/tool rows, and `PromptInput` never called `setCursorPosition`.
- RED evidence: in `.tmp-red-explorer-iter20`, `npm test -- test/core/testing/explorer/L1/invariants.extra.test.ts test/ui-auto/runFixtures.test.ts` failed because `nativeCursorDeclared` reported 0 violations for a fixture requiring a native cursor without telemetry.
- Root cause: `Messages` used Ink `<Static>` for completed turns, which moved previous chat content into terminal scrollback instead of keeping recent turns in the live conversation pane. `PromptInput` rendered a fake cursor glyph but did not declare a native Ink cursor position, so real terminal caret behavior could drift to the frame edge.
- Explorer root cause: the harness captured live frames and static writes but had no cursor telemetry or opt-in invariant, so a clean sweep could not distinguish a fake rendered cursor from a real positioned terminal cursor.
- Fix: `Messages` now renders recent turns only in the live transcript, bottom-aligned and clipped by the conversation zone. `PromptInput` uses Ink `useCursor()` with an absolute prompt-line position. The explorer now records cursor show escapes, passes `cursorTraces` through capture/sweep/fuzz/repair verification, and enforces `requiresNativeCursor` via `nativeCursorDeclared`.
- Skill update: `skills/ink-ui-explorer/SKILL.md` now documents the two runtime blind spots and was reinstalled into `/root/.codex/skills/ink-ui-explorer/SKILL.md`.
- Targeted tests: `npm test -- test/core/testing/explorer/L0/grid.test.ts test/core/testing/explorer/L1/invariants.extra.test.ts test/core/testing/explorer/sweep/sweep.test.ts test/ui-auto/runFixtures.test.ts test/tui/Messages.static.test.tsx test/tui/PromptInput.cursor.test.tsx test/tui/messages.test.tsx test/tui/promptInput.test.tsx test/tui/PromptInput.vim.test.tsx test/tui/PromptInput.mention.test.tsx test/tui/PromptInput.vimMention.test.tsx test/integration/chatRender.test.tsx` passed 63/63.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-20-final` reported 201 passed, 0 failed.
- Build/typecheck: `npm run build && npx tsc --noEmit && git diff --check` passed.
- Remaining default sweep failures: none.
- Follow-up: start the next iteration on a different Nuka surface; likely candidates are focused input overlays, tool-call progress rows, or model picker exit/restore behavior with real app process observation.
