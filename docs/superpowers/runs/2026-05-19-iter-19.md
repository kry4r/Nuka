# Iter-19 Run Record - 2026-05-19

- Surface: Explorer default sweep quarantine for intentionally red snapshot fixtures.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-19-baseline/` reported 200 passed, 7 failed. All failures were from `BugB-Snapshot`.
- RED commit: 5b60f41 `test(explorer/iter-19): expose snapshot fixture quarantine gap`
- GREEN commit: 81e251c `fix(explorer): skip explicit-only fixtures in CLI sweeps`
- RED evidence: `npm test -- test/cli/exploreSweep.test.ts` failed in the new CLI snapshot quarantine test because the CLI returned 1 instead of the expected 0 when a fixture declared `sweepMode: 'explicit-only'`.
- Root cause: `sweep()` had no way to distinguish normal regression fixtures from intentionally red snapshots. The default CLI sweep loaded every fixture, so legacy dogfood snapshots kept the whole suite permanently red even after the fixed regression fixture existed.
- Fix: fixture definitions now support `sweepMode: 'explicit-only'`; programmatic `sweep()` includes explicit-only fixtures by default, while the CLI passes `includeExplicitOnly: false`. A source-level legacy bridge treats `BugB-Snapshot` as explicit-only without editing the historical snapshot fixture in the GREEN commit.
- Programmatic dogfood behavior: `npm test -- test/cli/exploreSweep.test.ts test/core/testing/explorer/dogfood/bugB-sweep.test.ts` passed 7/7 and still observed the expected `BugB-Snapshot` failures through direct `sweep()` calls.
- Build: `npm run build` passed after the implementation change.
- Trellis check: applicable frontend spec and shared guide index were re-read; `git diff --check` passed.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-19-green-3/` reported 199 passed, 0 failed.
- Spec update: none needed; this is an explorer test harness behavior and the run note captures the compatibility rule.
- Remaining default sweep failures: none.
