# Iter-8 Run Record — 2026-05-19

- Surface: `SubagentDetail` plan approval text inside the nested approval box at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-8-baseline/` reported 176 passed, 9 failed.
- RED commit: 3d9a949 `test(ui-auto/iter-8): expose SubagentDetail CJK plan overflow`
- GREEN commit: cee4f0d `fix(tui/tasks): truncate subagent plans by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-8-red-check/` reported the new `SubagentDetailCjkPlan / cjk-plan-line-stays-inside-approval-box` case failing at 60×30 and 70×30; total moved to 176 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-8-source-red-check/` reported the same 176 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Tasks/SubagentDetail.test.tsx` passed 3/3.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-8-green-check/` reported the new case passing at 60×30 and 70×30; total returned to 178 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-8-source-green-check/` reported the same 178 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-8-subagent-detail-cjk-plan.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-8-subagent-capture-green/` showed the CJK plan line ending with an ellipsis inside the bordered approval box.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
