# Iter-21 Run Record - 2026-05-20

- Surface: `ink-ui-explorer` PATH shim plus `MentionPalette` preview footer rendering at narrow widths.
- Baseline sweep: `ink-ui-explorer sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-21-baseline` reported 201 passed, 0 failed.
- Skill RED commit: 9b67a2f `test(skill/iter-21): require ink explorer path shim`
- Skill GREEN commit: 08dd41e `fix(skill): expose ink explorer path shim`
- TUI RED commit: aa6ab55 `test(tui/iter-21): expose mention preview overflow`
- TUI GREEN commit: 5407d51 `fix(tui): truncate mention preview by viewport width`
- Skill RED evidence: in `.ink-explorer/.tmp-red-skill-iter21`, `HOME="$PWD/.ink-explorer/.tmp-red-skill-iter21/.tmp-home" npm --prefix .ink-explorer/.tmp-red-skill-iter21 test -- test/skills/installHook.test.ts` failed 2/4 because `package.json` did not expose `ink-ui-explorer` and `install-skills.mjs` did not link `~/.nuka/bin/ink-ui-explorer`.
- TUI RED evidence: in `.ink-explorer/.tmp-red-mention-iter21`, `node dist/cli.js explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-21-red-head` reported 201 passed, 2 failed. Both failures were `MentionPalettePreviewWidth / long-preview-stays-inside-viewport` at 60×24 and 70×24.
- Root cause: the skill shim existed under `skills/ink-ui-explorer/bin/` and installed skill folders, but the root package did not expose it as an npm bin and the installer did not place it in `~/.nuka/bin`. Separately, `MentionPalette` truncated result rows but rendered the optional preview footer without a viewport-derived width cap.
- Fix: root `package.json`/`package-lock.json` now expose `ink-ui-explorer`, and `scripts/install-skills.mjs` links the shim into `~/.nuka/bin`. `MentionPalette` now reads terminal columns via `useTerminalSize()` and truncates preview text with `truncateByWidth`.
- Green sweep: `ink-ui-explorer sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-21-green` reported 203 passed, 0 failed.
- Targeted checks: `npm test -- test/skills/installHook.test.ts test/skills/inkUiExplorerBin.test.ts test/ui-auto/runFixtures.test.ts test/tui/promptMentions/MentionPalette.test.tsx` passed 24/24.
- Build/typecheck: `npm run build && npx tsc --noEmit && git diff --check` passed.
- PATH check: `which ink-ui-explorer` resolved to `/root/.nvm/versions/node/v25.8.1/bin/ink-ui-explorer`, and `ink-ui-explorer --help` printed the `nuka explore` verb table.
- Remaining default sweep failures: none.
- Stop point: user requested pause after iter-21; do not start iter-22 until resumed.
