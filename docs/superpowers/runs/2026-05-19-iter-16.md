# Iter-16 Run Record — 2026-05-19

- Surface: onboarding `Wizard` verification error message at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-16-baseline/` reported 192 passed, 9 failed.
- RED commit: 4bdaad8 `test(ui-auto/iter-16): expose onboarding CJK error overflow`
- GREEN commit: 4a19b72 `fix(tui/onboarding): truncate error messages by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-16-red-check/` reported the new `OnboardingErrorCjkMessage / cjk-error-message-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 192 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-16-source-red-check/` reported the same 192 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/Onboarding/wizard.test.tsx` passed 6/6.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-16-green-check-2/` reported the new case passing at 60×30 and 70×30; total returned to 194 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-16-source-green-check/` reported the same 194 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-16-onboarding-error-cjk-message.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-16-onboarding-error-capture-green/` showed the CJK error message ending with an ellipsis inside the error frame.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires display-width helpers for visible truncation and alignment.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
