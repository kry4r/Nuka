# Iter-13 Run Record — 2026-05-19

- Surface: `MessageRow` / `ToolCall` generic tool-use input summary at narrow explorer viewports.
- Baseline sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-13-baseline/` reported 186 passed, 9 failed.
- RED commit: 75a771b `test(ui-auto/iter-13): expose MessageRow CJK tool input overflow`
- GREEN commit: ccc0718 `fix(tui/messages): truncate tool summaries by display width`
- RED evidence: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-13-red-check/` reported the new `MessageRowToolInputCjk / cjk-tool-input-summary-truncates-by-display-width` case failing at 60×30 and 70×30; total moved to 186 passed, 11 failed.
- Source RED evidence: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-13-source-red-check/` reported the same 186 passed, 11 failed.
- Build: `npm run build` passed after the implementation change.
- Targeted Vitest: `npm test -- test/tui/messages.test.tsx test/tui/toolCall.test.tsx test/tui/agentCall.test.tsx` passed 25/25.
- Explorer sweep: `nuka explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-13-green-check-2/` reported the new case passing at 60×30 and 70×30; total returned to 188 passed, 9 failed.
- Source-mode sweep: `node --import=tsx/esm src/cli.tsx explore sweep --fixture-root=test/ui-auto/fixtures --no-judge --out=.ink-explorer/iter-13-source-green-check-2/` reported the same 188 passed, 9 failed.
- Capture check: `node --import=tsx/esm src/cli.tsx explore capture test/ui-auto/fixtures/iter-13-message-row-tool-input-cjk.fixtures.tsx --viewport=60x30 --out=.ink-explorer/iter-13-message-row-tool-input-capture-green-2/` showed the CJK JSON input summary ending with an ellipsis on a single tool-call header row.
- Trellis check: applicable frontend spec was re-read; no spec update needed because `.trellis/spec/frontend/quality-guidelines.md` already requires `stringWidth` / `truncateByWidth` for visible terminal truncation.
- Remaining failures are unchanged baseline failures: Welcome CJK narrow-width cases plus Bug B snapshot cases.
