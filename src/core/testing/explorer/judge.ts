// src/core/testing/explorer/judge.ts
//
// L3' Judge verb — M4 implementation placeholder.
// See locked spec §4.5 for the full design.

import type { JudgeOpts, JudgeResult } from './types'

/**
 * Re-run only the Judge stage on the most recent sweep's grids.
 *
 * @throws {Error} not implemented (M4)
 */
export async function judge(_opts: JudgeOpts): Promise<JudgeResult> {
  throw new Error('not implemented (M4)')
}
